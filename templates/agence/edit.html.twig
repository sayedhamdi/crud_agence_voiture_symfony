{% extends 'base.html.twig' %}

{% block title %}Edit Agence{% endblock %}

{% block body %}
    <h1>Edit Agence</h1>

    {{ include('agence/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex mt-2">
    <a href="{{ path('agence_index') }}" class="btn btn-info mx-2">back to list</a>

    {{ include('agence/_delete_form.html.twig') }}
    </div>
{% endblock %}
